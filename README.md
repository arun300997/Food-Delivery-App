npm init // initialise npm 
npm install -D parcel // create parcel as a deev dependency inside the app
npm parcel index.html //run the app on the local server
npm parcel build index.html //create a production ready app
