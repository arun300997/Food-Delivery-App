npm init // initialise npm 
npm install -D parcel // create parcel as a deev dependency inside the app
npm parcel index.html //run the app on the local server
npm parcel build index.html //create a production ready app

# Setting up Testing 

Installed  react testing library
installed jest
installed babel dependencies
configured babel
configure .parcelsrc to disable Babel transpilation
jest configuration (npm init jest@latest)
install jsdom library
install @babel/preset-react - to make jsx work in test casee.
include @babel/preset-react in my babel configuration.
install @testing-library/jest-dom.
